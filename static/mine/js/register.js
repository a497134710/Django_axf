$(function () {    fail(false)    //失去光标    //用户发起Ajax请求，看账户是否能用    $("#account").blur(function () {        var account = $(this).val()        var $that  =  $(this)    //   发起Ajax请求        $.get('/checkuser/',{"account":account},function (response) {            if (response["status"] == 0){                   $("#formAccount").addClass('form-group has-error has-feedback')                  $($that).next().addClass('glyphicon glyphicon-remove form-control-feedback')                $('#formAccount').next().html("用户名已存在")            } else {              $("#formAccount").removeClass('form-group has-error has-feedback')                $($that).next().removeClass('glyphicon glyphicon-remove form-control-feedback')                $('#formAccount').next().html("")            }        })    })    $('#password').blur(function () {        if ($(this).val() == '' || $(this).val().length < 6){            $("#formPassword").addClass('form-group has-error has-feedback')             $(this).next().addClass('glyphicon glyphicon-remove form-control-feedback')                $('#formPassword').next().html("密码不能为空且不能少于6位")        }else {             $("#formPassword").removeClass('form-group has-error has-feedback')                $(this).next().removeClass('glyphicon glyphicon-remove form-control-feedback')                $('#formPassword').next().html("")        }    })    $("#mypassword").blur(function () {        //获取2次的密码进行对比        var  password = $('#password').val()        var  mypassword = $("#mypassword").val()        if (mypassword != password){            $("#formMypassword").addClass('form-group has-error has-feedback')             $(this).next().addClass('glyphicon glyphicon-remove form-control-feedback')                $('#formMypassword').next().html("两次密码输入不一致")        }else {             $("#formMypassword").removeClass('form-group has-error has-feedback')                $(this).next().removeClass('glyphicon glyphicon-remove form-control-feedback')                $('#formMypassword').next().html("")            if ($("#account").val() != ''){                 fail(true)            }        }    })    //如果不符合注册条件，让注册按钮失效    function fail(status) {            if (status == true){                $('#submit').removeAttr('disabled')                $("#submit").addClass("active")            }else {                $('#submit').attr('disabled','disabled')            }    }})