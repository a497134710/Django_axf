$(function () {    var typeIndex = $.cookie("typeIndex")    if (typeIndex) {        $('.type-slider li').eq(typeIndex).addClass('action')    } else { // 如果不存在默认热榜，这个typeIndex是为了之后的Url中的参数准备的        $('.type-slider li:first').addClass('action')    }    $('.type-slider li').click(function () {        // $('.type-slider li').removeClass("action")        // $(this).addClass("action")        var typeIndex1 = $(this).index()        $.cookie("typeIndex", typeIndex1, {exprires: 3, path: '/'})    })    var type = false;    $('#type1').click(function () {        type = !type;        if (type) {            sort1 = false            $('#type ').show()            $('#sort').hide()            $('#type1 i').removeClass('glyphicon-chevron-up').addClass('glyphicon-chevron-down')            $('#sort1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')        } else {            $('#type').hide()            $('#type1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')        }    })    var sort1 = false    $("#sort1").click(function () {        sort1 = !sort1;        if (sort1) {            $('#sort ').show()            type = false            $('#type').hide()            $('#sort1 i').removeClass('glyphicon-chevron-up').addClass('glyphicon-chevron-down')            $('#type1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')        } else {            $('#sort').hide()            $('#sort1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')        }    })    $('.bounce-view').click(function () {        $('#sort').hide()        $('#type').hide()        $('#type1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')            $('#sort1 i').removeClass('glyphicon-chevron-down').addClass('glyphicon-chevron-up')    })// 购物车操作    // 默认隐藏    $('.bt-wrapper>.glyphicon-minus').hide()    $('.bt-wrapper>.num').hide()    // 购物车数据不为空，即显示    // each 遍历操作    $('.bt-wrapper>.num').each(function () {        if(parseInt($(this).html())){            $(this).show()            $(this).prev().show()        }    })//    加到购物车    $('.glyphicon.glyphicon-plus').click(function () {    //    发起请求        var  goodid = $(this).attr("pid")        var $that = $(this)        var num = $('.num').html()        $.get('/addcart/',{"goodid":goodid ,"num":num},function (response) {            if(response["status"] ==0){                window.open('/login/',target="_self")            }            else {               $($that).prev().html(response["number"])                $($that).prev().prev().show()                $($that).prev().show()            }        })    })//    移出购物车    $('.bt-wrapper .glyphicon.glyphicon-minus').click(function () {        var  goodid = $(this).attr("pid")        var  that = $(this)        $.get('/reducecart/',{'goodid':goodid},function (response) {               if (response["status"] == 1){                   // $(that).next().html(response["number"])                   var  number = response["number"]                   if (number>0){                       $(that).show()                       $(that).next().html(number)                   }                   else {                       $(that).hide()                       $(that).next().hide()                   }               }        })    })})